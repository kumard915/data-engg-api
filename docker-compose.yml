version: "3.8"

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.3.0
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: "zookeeper:2181"
      KAFKA_LISTENERS: "PLAINTEXT://0.0.0.0:9092"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://localhost:9092"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  node-app:
    build: .
    container_name: mock-data-generator
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - EVENTHUB_NAMESPACE=de-eventhub-demo
      - EVENTHUB_NAME=payin-events
      - EVENTHUB_CONNSTRING=
    depends_on:
      - kafka
    volumes:
      - .:/app
    command: npm run dev
# version: '2'
# services:
#   zookeeper:
#     image: confluentinc/cp-zookeeper:7.3.0
#     environment:
#       ZOOKEEPER_CLIENT_PORT: 2181
#       ZOOKEEPER_TICK_TIME: 2000
#     ports:
#       - "2181:2181"

#   kafka:
#     image: confluentinc/cp-kafka:7.3.0
#     depends_on:
#       - zookeeper
#     ports:
#       - "9092:9092"
#     environment:
#       KAFKA_BROKER_ID: 1
#       KAFKA_ZOOKEEPER_CONNECT: 'zookeeper:2181'
#       KAFKA_LISTENERS: 'PLAINTEXT://0.0.0.0:9092'
#       KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://localhost:9092'
#       KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
